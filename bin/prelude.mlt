id : ;
ifte : ( ( ( dup ) dip i ) dip ) dip ? i ;
pop : () ky ; 
dip : cy ky ;
cons : cy pop ;
i : (()) dip ky ;
dup : () cy dip dip ;
cat : ((i) dip i) cons cons ;
unit : () cons  ;
swap : unit dip ;
swapd : ( swap ) dip ;
rollup : swap swapd ;
popd : ( pop ) dip ;
dupd : ( dup ) dip ;
not : f t ? ;
and : () (pop f) ? i;
or : (pop t) () ? i;
xor : (not) () ? i;
nor : or not ;
zero? : 0 = ; 
pred : 1 - ;
succ : 1 + ;